# This file is @generated by <https://github.com/liblaf/copier-release>.
# DO NOT EDIT!

name: Release / Publish

on:
  schedule:
    - cron: "0 * * * *" # Runs every hour
  workflow_dispatch:

jobs:
  release-publish:
    name: Publish Release
    permissions:
      contents: write
    runs-on: ubuntu-latest
    environment:
      name: Release Please
      deployment: false
    steps:
      - id: auth
        name: Auth
        uses: liblaf/actions/auth@v1
        with:
          app-id: ${{ vars.APP_ID }}
          private-key: ${{ secrets.PRIVATE_KEY }}
      - name: Publish drafts
        uses: liblaf/actions/publish-drafts@v1
        with:
          older-than: 6h
          token: ${{ steps.auth.outputs.token }}
