# ref: <https://copier.readthedocs.io/en/stable/configuring/>

_answers_file: .config/copier/.copier-answers.release.yaml

_envops:
  comment_start_string: "# {#"

_migrations:
  - command: rm --force --recursive '.config/release-please/'
    version: v0.2.0
  - command: rm --force --recursive '.github/workflows/release-please.yaml'
    version: v0.2.0

_subdirectory: template
