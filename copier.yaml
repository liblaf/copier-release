# ref: <https://copier.readthedocs.io/en/stable/configuring/>

_answers_file: .config/copier/.copier-answers.release.yaml

_subdirectory: template

_tasks:
  - rm --force --recursive '.config/release-please/'
  - rm --force '.github/workflows/release-create.yaml'
  - rm --force '.github/workflows/release-please.yaml'
